fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.0
pydantic-settings>=2.7
anthropic>=0.50
openai>=1.0
qdrant-client>=1.13
sqlmodel>=0.0.21
asyncpg>=0.30
cryptography>=42
python-docx>=1.1
markdown>=3.5
# weasyprint is excluded — it requires system libraries (Cairo, Pango) that are
# not available in Vercel's Lambda environment.  The /export/pdf endpoint returns
# HTTP 503 gracefully when weasyprint is not installed.
